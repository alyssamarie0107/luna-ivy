import styles from './HorizontalLine.module.css';

const HorizontalLine = () => {
    return (
        <div id={ styles.HorizontalLineContainer }>
            <div id={ styles.HorizontalLine }></div>
        </div>
    );
};

export default HorizontalLine;